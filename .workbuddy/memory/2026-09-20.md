# 2026-09-20 工作日志

## 小鹏日报看板同步推送（自动化 automation-1787760340133）
- 运行 deploy.py：新增 `小鹏运营日报_2026-09-19.html`（44.8 KB），data.json 更新为 **62 日报 + 3 月报 = 65 份**
- git 提交 `9741d93`「更新日报 2026-09-19」（4 files，468 insertions）
- ⚠️ **推送方案变更（重要）**：本次**直连方案首次失败**——`Recv failure: Connection was reset`（github.com:443 被重置），说明本机直连 GitHub 已不再稳定
- ✅ 回退走本地代理 **127.0.0.1:7890（Clash 已启动，LISTENING）** 推送成功：`19367e5..9741d93`
  - 注意：回退方案凭据仍用 store（`-c credential.helper= -c credential.helper=store`），**未用 wincred**，避免 GCM 弹窗
- 验证：线上 data.json 最新日期 = **2026-09-19** ✅（HTTP 200，11180 bytes，65 条），报告页 `reports/小鹏运营日报_2026-09-19.html` HTTP 200 / 44844 bytes，title 正确
- 校验：本地 HEAD 与 `git ls-remote origin main` 均为 `9741d93`，工作区干净
- 📌 结论：直连不再是可靠默认，**下次应「先试直连，失败即回退 7890」**；7890 可用性需每次用 `netstat | grep 7890 | grep LISTENING` 确认
