# 2026-09-19 工作日志

## 小鹏日报看板同步推送（automation-1787760340133，✅成功）
- `python deploy.py`：新增 `reports/小鹏运营日报_2026-09-18.html`（44.5 KB，上游 06:03 生成），data.json 更新为 61 日报 + 3 月报 = 64 份
- git commit `19367e5`「更新日报 2026-09-18」（data.json + 新日报，2 files，448 insertions）
- 推送：直连方案一次成功（unset 6 条代理变量 + store 凭据 + `-c http.proxy= -c https.proxy=`），`074eece..19367e5`，未依赖 Clash/7890
- 线上验证：https://xpeng-report-dashboard.pages.dev/data.json 最新日期 = 2026-09-18（HTTP 200，64 份；等待 50 秒后首个轮询即生效）
- 一致性校验：本地 HEAD 与 `git ls-remote origin main` 均为 `19367e5`，工作区干净
- 上游已连续 7 天正常产出（9-12~9-18），直连方案连续 8 天稳定

## 小坑记录
- `curl -o /tmp/live.json` 在本机 Git Bash 下写不进 /tmp（HTTP 200 但文件不存在）→ 改为 `-o` 输出到当前工作目录，验证成功后手动 `rm` 清理
