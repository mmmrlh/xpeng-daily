# 2026-09-12 工作日志

## 小鹏日报同步推送（自动化 automation-1787760340133）
- deploy.py 同步成功：新增 小鹏运营日报_2026-09-11.html，data.json 更新为 54 日报+3 月报=57 份
- git commit `911ad7a` "更新日报 2026-09-11"（3 files）
- **推送方式变更**：本地 Clash 代理 127.0.0.1:7890 本次未运行，原「走 7890 + wincred」命令失败；直连 + wincred/GCM 会挂起（GCM 无凭据弹窗卡死）
- 改用 store 凭据 + 直连（不带 proxy）推送成功：b22e13f..911ad7a
  - 命令：`git -c credential.helper= -c credential.helper=store -c credential.interactive=false -c credential.modalPrompt=false -c http.proxy= -c https.proxy= push origin main`
  - 依据：`~/.git-credentials` 存在且可用；本机直连 GitHub 的 GET（ls-remote/fetch）与 POST（push）均正常
- Cloudflare Pages 验证通过：data.json 最新日期 = 2026-09-11 ✅（首次轮询即生效）

## 环境备忘
- 本机 7890 代理端口未监听；旁路由 192.168.31.254 / 主路由 192.168.31.1 的常见代理端口也均未开放
- 网关 192.168.31.1 可达（本地网络正常），DNS 解析正常
- 全局 git credential.helper = GCM（git-credential-manager.exe），无人值守下会挂起；优先用 `-c credential.helper= -c credential.helper=store` 覆盖
