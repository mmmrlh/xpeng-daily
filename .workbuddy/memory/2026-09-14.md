# 2026-09-14

## 小鹏日报看板同步推送（automation-1787760340133）

- 运行 `python deploy.py`：**没有新日报/月报需要同步**。data.json 仍为 54 日报 + 3 月报 = 57 份，最新日报 2026-09-11。
- 排查：源目录 `小鹏运营日报/outputs/` 最新文件时间戳仍为 **9/12 06:03**（`小鹏运营日报_2026-09-11.html`），**9/13、9/14 均无任何新文件** → 上游日报采集/生成流程自 9/12 起**连续 2 天未产出**，需人工检查日报采集任务。
- git：无日报内容变更（`git diff --cached --quiet` 判定为 NO_CHANGES），仅提交运行记录类变更（memory 文件）。
- 推送：任务指定的 7890 代理方案**不可用**（`netstat` 显示 127.0.0.1:7890 无 LISTENING 监听，Clash 未运行）→ 改用 store 凭据 + 直连推送成功。
- 验证：线上 `https://xpeng-report-dashboard.pages.dev/data.json` 最新日期 = **2026-09-11**（HTTP 200，57 份），**非昨天（2026-09-13）**——因为确实没有 9-12 / 9-13 的日报。

## 推送方式切换为直连（用户确认后执行）

- 用户问"为什么同步需要开 Clash"，解释根因（GitHub 直连历史上被重置 → 旧命令写死走 7890）后确认改为直连。
- ✅ **自动化 automation-1787760340133 的 prompt 已更新**：第 4 步改为 store 凭据 + 直连推送为首选（`-c credential.helper= -c credential.helper=store -c http.proxy= -c https.proxy=`），7890 代理降级为兜底；第 5 步验证 curl 前也要求 unset 代理变量。
- ✅ 新建技能 `~/.workbuddy/skills/xpeng-dashboard-sync/SKILL.md`：完整固化同步→提交→直连推送→线上验证流程 + 坑点（GCM 弹窗挂起、残留代理变量、7890 仅兜底、上游连续无产出需查采集流程、Mac 端差异）。
- 项目 MEMORY.md 同步更新：直连方案标记为"已固化为自动化默认"。

## 补同步 9-12、9-13 日报（用户手动跑上游后，✅成功）

- 08:46/08:51 上游补产出 `小鹏运营日报_2026-09-12.html`、`小鹏运营日报_2026-09-13.html` → deploy.py 同步，data.json 变为 **56 日报 + 3 月报 = 59 份**
- git：commit `8fdd24c`「补同步日报 2026-09-12、2026-09-13」，直连推送成功 `ab44e0e..8fdd24c`
- 验证：线上 https://xpeng-report-dashboard.pages.dev/data.json 最新日期 = **2026-09-13** ✅（首次轮询即生效）
- ⚠️ **当日本机 shell 环境故障与修复**（重要经验）：
  - 症状：WorkBuddy bash shim 启动报 `dirname: command not found`，PATH 全空，`ls`/`grep`/`git` 全找不到；PowerShell 里 `where.exe git` 也找不到 git
  - git push 表现为**静默退出 128**（GIT_TRACE 显示卡在 `git credential-store get` 子进程，PATH 缺失导致 sh.exe 子进程起不来）
  - 修复：每条命令前显式补 PATH：`export PATH="/c/Users/TXair/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin:/c/Users/TXair/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Windows/System32:$PATH"`
  - python 用完整路径 `C:\Users\TXair\.workbuddy\binaries\python\versions\3.13.12\python.exe`
  - PowerShell 的 `>` 重定向输出为 UTF-16，Read 工具会判为二进制；且错误输出捕获不可靠 → 复杂命令一律写入文件再 Read，或优先用 bash（记得补 PATH）
- 已将"PATH 损坏时补 PATH 再推送"写入技能 xpeng-dashboard-sync 的坑点清单
